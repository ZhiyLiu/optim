// Value passed to Pointlist_server2::init().
extern const int MEDIAL_SUBDIVISIONS = 5;			/* Plain tiles */
extern const int SUBDIV_MEDIAL_SUBDIVISIONS = 4;	/* Subdivision tiles */

// Value passed to most calls of
// Pointlist_server2::ComputeTilePointCloud().
extern const int POINT_CLOUD_SUBDIVISIONS = 5;

// Value passed to Pointlist_server2::ComputeTilePointCloud()
// in class Match.
extern const int MATCH_POINT_CLOUD_SUBDIVISIONS = 2;

// Multiplier of level-of-detail value in surface renderer.  This
// times the level is passed to Pointlist_server2::init().
extern const int SURFACE_RESOLUTION_FACTOR = 4;

// Bias of level-of-detail value in constraints rendering.  This
// plus the level is passed to Pointlist_server2::init().
extern const int CONSTRAINTS_RESOLUTION_BIAS = 0;

// Used in M3DObjectRenderer.cpp.
// Must correspond to slider maxima in P3DUserInterface.fl.
extern const int ATOM_VECTORS_WIDTH_MAX = 10;
extern const int MESH_CONNECTORS_WIDTH_MAX = 10;

