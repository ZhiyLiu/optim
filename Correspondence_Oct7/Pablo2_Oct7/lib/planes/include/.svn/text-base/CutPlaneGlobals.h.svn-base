#ifndef CUT_PLANE_GLOBALS_H
#define CUT_PLANE_GLOBALS_H

#include "globalBuildParms.h"
#include "Vector3D.h"


//	Global definitions for the cut-plane visualization library.



#ifndef Point
#define Point	Vector3D
#endif

// Names used internally by the cut planes package.
enum plane_t { b_n, b_bperp, bperp_n, bperp_y0, bperp_y1, bperp_n_chord };

// Names used in the user interface of Pablo.
// This enumeration must be castable into the one above.
enum cut_plane_t {ATOM_PLANE, CREST_PLANE, BPERP_N_PLANE,
        STARBOARD_PLANE, PORT_PLANE, INVOLUTES_PLANE};

class CutPlanes;
class P3DCutPlaneView;




#endif	/* CUT_PLANE_GLOBALS_H */

