#-----------------------------------------------------------------------------
#
# SRepConfig.cmake - CMake configuration file for external projects.
# This file was automatically generated by the cmake macro 
# CREA_INSTALL_LIBRARY_FOR_CMAKE of the package CREA
#
# This file is configured by cmake and used by the
# UseSRep.cmake module to load the lib settings 
# for an external project.

# Build tree config ?
SET(CILC_BUILD_TREE_CONFIGURATION FALSE)


IF(UNIX)
SET(GOTO_INSTALL_PREFIX /../../..)
ENDIF(UNIX)


# The SRep include file *RELATIVE* directories.
SET(CILC_RELATIVE_INCLUDE_PATHS "include/SRep")
# Compute the prefix for include and library paths
IF(CILC_BUILD_TREE_CONFIGURATION)
  # In build tree 
  # the include paths are relative to the source tree *AND* the binary tree 
  # for generated files 
  SET(CILC_INCLUDE_PATH_PREFIX /playpen/workspace/newuoa/Correspondence_Oct7/Pablo2_Oct7)
  # Build the *ABSOLUTE* directories
  FOREACH(path ${CILC_RELATIVE_INCLUDE_PATHS})
    SET(SRep_INCLUDE_DIRS
      ${SRep_INCLUDE_DIRS}
      ${CILC_INCLUDE_PATH_PREFIX}/${path}
      )
  ENDFOREACH(path ${CILC_RELATIVE_INCLUDE_PATHS})
  SET(CILC_INCLUDE_PATH_PREFIX /playpen/workspace/newuoa/Correspondence_Oct7/Pablo2_Oct7/cmake-build-debug)
  # Build the *ABSOLUTE* directories
  FOREACH(path ${CILC_RELATIVE_INCLUDE_PATHS})
    SET(SRep_INCLUDE_DIRS
      ${SRep_INCLUDE_DIRS}
      ${CILC_INCLUDE_PATH_PREFIX}/${path}
      )
  ENDFOREACH(path ${CILC_RELATIVE_INCLUDE_PATHS})
ELSE(CILC_BUILD_TREE_CONFIGURATION)
  # In install tree 
  # the include paths are relative to install prefix 
  # On unix , GOTO_INSTALL_PREFIX allows to get back to the 
  # installation prefix from  SRep_DIR
  SET(CILC_INCLUDE_PATH_PREFIX ${SRep_DIR}${GOTO_INSTALL_PREFIX})
  # Build the *ABSOLUTE* directories
  FOREACH(path ${CILC_RELATIVE_INCLUDE_PATHS})
    SET(SRep_INCLUDE_DIRS
      ${SRep_INCLUDE_DIRS}
      ${CILC_INCLUDE_PATH_PREFIX}/${path}
      )
  ENDFOREACH(path ${CILC_RELATIVE_INCLUDE_PATHS})
ENDIF(CILC_BUILD_TREE_CONFIGURATION)



# Compute the prefix for library paths
IF(CILC_BUILD_TREE_CONFIGURATION)
  # In build tree
  # the library paths are relative to the binary tree 
  SET(CILC_LIBRARY_PATH_PREFIX /playpen/workspace/newuoa/Correspondence_Oct7/Pablo2_Oct7/cmake-build-debug)
ELSE(CILC_BUILD_TREE_CONFIGURATION)
  # In install tree 
  # the library paths are relative to install prefix
  SET(CILC_LIBRARY_PATH_PREFIX ${SRep_DIR}${GOTO_INSTALL_PREFIX})
ENDIF(CILC_BUILD_TREE_CONFIGURATION)
# The SRep library file *RELATIVE* directories.
SET(CILC_RELATIVE_LIBRARY_PATHS "lib64")
# Build the *ABSOLUTE* directories
FOREACH(path ${CILC_RELATIVE_LIBRARY_PATHS})
  SET(SRep_LIBRARY_DIRS
    ${SRep_LIBRARY_DIRS}
    ${CILC_LIBRARY_PATH_PREFIX}/${path}
    )
ENDFOREACH(path ${CILC_RELATIVE_LIBRARY_PATHS})

# Set the "prefix path"
SET(SRep_INSTALL_PREFIX ${CILC_LIBRARY_PATH_PREFIX})

# The C and C++ flags added by SRep to the cmake-configured flags.
SET(SRep_REQUIRED_C_FLAGS "")
SET(SRep_REQUIRED_CXX_FLAGS "")
SET(SRep_REQUIRED_LINK_FLAGS "")

# The SRep version 
SET(SRep_MAJOR_VERSION )
SET(SRep_MINOR_VERSION )
SET(SRep_BUILD_VERSION )
SET(SRep_VERSION ..)

# The location of the UseSRep.cmake file.
SET(SRep_USE_FILE "${SRep_DIR}/UseSRep.cmake")

# The build settings file.
SET(SRep_BUILD_SETTINGS_FILE 
  "${SRep_DIR}/SRepBuildSettings.cmake")

# A list of all libraries for SRep.  Those listed here should
# automatically pull in their dependencies.
SET(SRep_LIBRARIES SRep)

# Messages
IF(CREA_VERBOSE_CMAKE)
  MESSAGE(STATUS "=======================================")
  MESSAGE(STATUS "Looking for SRep... found:")
  MESSAGE(STATUS "* SRep_DIR          = ${SRep_DIR}")
  MESSAGE(STATUS "* SRep_VERSION      = ${SRep_VERSION}")
  MESSAGE(STATUS "* SRep_USE_FILE     = ${SRep_USE_FILE}")

  MESSAGE(STATUS "* SRep_INCLUDE_DIRS = ${SRep_INCLUDE_DIRS}")
  MESSAGE(STATUS "* SRep_LIBRARY_DIRS = ${SRep_LIBRARY_DIRS}")
  MESSAGE(STATUS "* SRep_LIBRARIES    = ${SRep_LIBRARIES}")
ENDIF(CREA_VERBOSE_CMAKE)

# Does the library has an additional config file (user provided) ?
SET(SRep_HAS_ADDITIONAL_CONFIG_FILE FALSE)

IF (SRep_HAS_ADDITIONAL_CONFIG_FILE)
  IF(CREA_VERBOSE_CMAKE)
    MESSAGE(STATUS "Reading SRep additional configuration file")
  ENDIF(CREA_VERBOSE_CMAKE)
  # Include it
  INCLUDE(${SRep_DIR}/AdditionalSRepConfig.cmake)
ENDIF (SRep_HAS_ADDITIONAL_CONFIG_FILE)
